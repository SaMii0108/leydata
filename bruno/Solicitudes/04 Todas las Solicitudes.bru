meta {
  name: Todas las Solicitudes
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/purpose-requests
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Lista todas las solicitudes independientemente del estado (PENDING, APPROVED, REJECTED).
  Requiere rol DPO o ADMIN.
}
